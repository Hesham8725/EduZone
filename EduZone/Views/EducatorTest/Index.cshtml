@model IEnumerable<EduZone.Models.Educator>

@{
    ViewBag.Title = "Index";
}

@*<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AcademicDegree)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccountID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AcademicDegree)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccountID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}
</table>*@

<div class=" d-flex rounded-2 mt-3">
    <div class="border shadow  rounded-3 p-2" style="width: 40%">
        <form action="">
            <h1>Add Educator Email</h1>
            <label class="form-label fw-semibold mt-2">Email</label>
            <input class="form-control" type="text" name="email">

            <input type="submit" value="Add" class="btn btn-success mt-2">
        </form>
    </div>
    <div class="w-75">
        <table class="table shadow table-hover table-responsive table-striped">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Email</th>
                    <th scope="col">More</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <th scope="row">aaaa</th>
                    <td>Hesham.20353601@compit.aun.edu.eg</td>
                    <td>
                        <a href="/Admin/show/aaaaa" class="btn btn-outline-success">Update</a>
                        <a href="/Admin/Delete/aaaaa" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>