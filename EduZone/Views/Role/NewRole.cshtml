@using Microsoft.AspNet.Identity;

@model EduZone.Models.RoleViewModel
@{
    ViewBag.Title = "NewRole";
    Layout = null;
}

@using (Html.BeginForm())
{
    @Html.TextBoxFor(e => e.RoleName, "", new { });

    <input type="submit" value="Add Role">
    @User.Identity.GetUserId();
    
}

